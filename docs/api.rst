API Reference
=============

The main entry point for matplotly is the :func:`matplotly.matplotly` function.
It can be used as a direct call, a decorator, or a context manager — see
:doc:`quickstart` for usage examples.

.. autofunction:: matplotly.matplotly
